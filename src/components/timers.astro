---
import Root from "../layouts/root.astro";

const props = Astro.props
---

  <div class:list={["flex-grow flex flex-col gap-2 text-green-500", props.class]}>
    <span class="text-2xl">Qwark</span>
    {[1,2,3].map(n => (
      <div class="bg-base-200 w-24 aspect-square flex justify-center items-center text-2xl">{n}</div>
    ))}
    </div>
